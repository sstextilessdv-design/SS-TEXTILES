import ImageKit from imagekit const imagekit = new ImageKit { publicKey: process.env.IMAGEKIT_PUBLIC_KEY, privateKey: process.env.IMAGEKIT_PRIVATE_KEY, urlEndpoint: process .env.IMAGEKIT_URL_ENDPOINT, } export default async function handler(req, res) [ // Get authenres.status (200).json(result);tication parametersconst result = imagekit.getAuthenticationParameters();`
